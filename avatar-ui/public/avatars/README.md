# Avatar Assets

Place `.vrm` files and related avatar assets here.

The current prototype loads:

```text
public/avatars/avatar.vrm
```

Keep that filename when replacing the active model, or update `AVATAR_URL` in `src/components/AvatarViewer.tsx`.
